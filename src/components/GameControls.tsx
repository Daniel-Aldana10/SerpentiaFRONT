import React from 'react';

const GameControls: React.FC = () => (
  <div style={{ marginTop: 20 }}>
    <h4>Controles:</h4>
    <div>
      <kbd>W</kbd> Arriba<br />
      <kbd>A</kbd> Izquierda<br />
      <kbd>S</kbd> Abajo<br />
      <kbd>D</kbd> Derecha
    </div>
  </div>
);

export default GameControls; 